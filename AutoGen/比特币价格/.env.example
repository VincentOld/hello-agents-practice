# 用法：复制本文件为 .env，把 LLM_API_KEY 换成你的真实 key 后使用。
# .env 已被 .gitignore 忽略，不会被提交，密钥安全。
#
# 这里走的是「OpenAI 兼容」接口。DeepSeek 提供 OpenAI 格式的 API，
# 所以只要把下面三项指向 DeepSeek 即可，代码一行都不用改。

LLM_MODEL_ID=deepseek-chat
LLM_API_KEY=在这里填你的-DeepSeek-API-KEY
LLM_BASE_URL=https://api.deepseek.com/v1
